import subprocess

# Run the first Python file
subprocess.run(["python", "app.py"])

# After the first file is done executing, run the second Python file
subprocess.run(["python", "diseasedetector.py"])
